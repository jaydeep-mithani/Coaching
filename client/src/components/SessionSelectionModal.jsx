import React from "react";
import { MainModal } from "./MainModal";
const SessionSelectionModal = ({ open, handleClose }) => {
  return <MainModal open={open} handleClose={handleClose}></MainModal>;
};

export default SessionSelectionModal;
